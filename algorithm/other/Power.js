/**
 * @topic: 给定一个double类型的浮点数base和int类型的整数exponent。求base的exponent次方
 * @Date: 2019-06-22
 */
function power(base, exponent)
{
    return Math.pow(base, exponent);
}

let value = power(3.0, 2);
console.log(value);
